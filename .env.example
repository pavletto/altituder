# Altituder Configuration
# This file shows all available environment variables.
# Copy this to .env and adjust values as needed.

# Cache directory for DEM tiles
DDM_CACHE_DIR=./cache

# URL template for downloading tiles
# Use {s} for subdomain, {z} for zoom, {x} and {y} for tile coordinates
DDM_URL_TEMPLATE=https://{s}.geodata.microavia.com/srtm/{z}/{y}/{x}.ddm

# Comma-separated list of subdomains for load balancing
DDM_SUBDOMAINS=a,b,c

# Default zoom level (higher = more detail)
DDM_DEFAULT_Z=14

# Maximum native zoom level available from the tile source
DDM_MAX_NATIVE_Z=14

# Height multiplication factor (use to convert units if needed)
DDM_HEIGHT_FACTOR=1.0

# Comma-separated list of no-data values to ignore
# Example: -32768,3.4028235e+38
DDM_NODATA_CSV=

# HTTP server address (for 'serve' command)
ADDR=:8080
